import React from 'react';

const MainImage =({imageTitle}) => {
    return(
        <div className="elem-border">
                <h1 id="mainTitle">{imageTitle}</h1> 
        </div>
    )
};

export default MainImage;